{
  "config": {
    "step": {
      "pick_implementation": {
        "title": "YouTube"
      },
      "auth": {
        "title": "Connect YouTube"
      }
    },
    "abort": {
      "oauth2_failed": "YouTube authorization failed.",
      "oauth2_unauthorized": "YouTube authorization was rejected.",
      "missing_credentials": "Add Google OAuth credentials first."
    }
  },
  "application_credentials": {
    "description": "Create Google OAuth credentials at {console_url}. Use a Web application OAuth client and add the Home Assistant OAuth redirect URI shown by Home Assistant."
  },
  "options": {
    "step": {
      "init": {
        "title": "YouTube Playlists",
        "description": "Choose which playlists to import, and how clicking a video should play it.",
        "data": {
          "playlist_filter_mode": "Playlists to import",
          "playlist_pattern": "Pattern (e.g. HA* for a prefix, *Podcast* to match anywhere)",
          "play_target_mode": "How to play videos"
        }
      },
      "script": {
        "title": "Script playback",
        "description": "This script is called with a video_id when a video is clicked.",
        "data": {
          "play_script": "Script to run"
        }
      },
      "media_player": {
        "title": "Media player playback",
        "description": "Choose a media player that can play YouTube URLs, such as a Music Assistant player. It will be turned on if it's off, the volume will be set, then the video will launch automatically.",
        "data": {
          "play_media_player": "Music player",
          "play_default_target": "Default playback destination",
          "play_tv": "TV (optional, for long-press playback)",
          "play_power_on_entity": "Entity to use for turning on the player",
          "play_volume": "Volume to set before playing"
        }
      }
    },
    "error": {
      "pattern_required": "Enter a pattern, or switch back to \"All playlists\".",
      "media_player_required": "Choose a media player.",
      "tv_required_for_default": "Choose a TV when TV is the default destination.",
      "script_required": "Choose a script."
    }
  },
  "selector": {
    "playlist_filter_mode": {
      "options": {
        "all": "All playlists",
        "pattern": "Match a pattern / prefix"
      }
    },
    "play_target_mode": {
      "options": {
        "script": "Run a script when a video is clicked",
        "media_player": "Play directly on a media player entity"
      }
    }
  }
}
